// Re-export PhantomProvider under the legacy SolanaProviders name
// so layout.tsx doesn't need updating for the import path.
export { PhantomProvider as SolanaProviders } from "@/components/providers/PhantomProvider";
